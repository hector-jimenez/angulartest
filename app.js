(function () {
  'use strict'; // best practise

  angular.module('testApp', []) // brackets are dependencies, this bounds the app to the View
    .controller('Controller1', function () { // this bounds the controller's function to the div in the view

    })
} )();
